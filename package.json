{
  "name": "kiroclone",
  "version": "0.1.0",
  "private": true,
  "description": "KiroClone â€“ An agentic AI-powered IDE (Phase 1 scaffold)",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "echo \"(Add ESLint later)\"",
    "test": "echo \"(Add Jest/Playwright later)\""
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.9.3"
  }
}
